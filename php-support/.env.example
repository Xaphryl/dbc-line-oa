# Copy this to .env in the same folder on Hostinger (public_html/line-oa/.env)
# NEVER commit the real .env to git

# MySQL — same DB as nsync (copy values from public_html/nsync/api/.env on Hostinger)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password

# LINE OA shared API key (same value set in wrangler secret LINE_OA_API_KEY)
LINE_OA_API_KEY=LSqhZJHf3Q24NBgFKInkiU57aVrPbdT1zljDp0OX

# Image base URL
IMAGE_BASE_URL=https://dentalbuddyclinic.com/line-oa/images
